/* Requires the Docker Pipeline plugin */ 
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                bat 'echo "The pipeline is running xoxoxo"'
                bat 'python --version'
            }
        }
    }
}
