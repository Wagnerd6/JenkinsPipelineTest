/* Requires the Docker Pipeline plugin */ 
node {
    checkout scm
    
    stage('Build') {
        echo "Build stage"
    }
    stage('Test') {
        echo "Test stage"
    }
    stage('Deploy') {
        echo "Deploy stage"
    }
}
