/* Requires the Docker Pipeline plugin */ 
node {
    stage('Build') {
        echo "Build stage"
    }
    stage('Test') {
        echo "Test stage"
    }
    stage('Deploy') {
        echo "Deploy stage"
    }
}
