/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "The pipeline is running xoxoxo"'
                sh 'python --version'
            }
        }
    }
}
